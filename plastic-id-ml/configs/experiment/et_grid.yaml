data:
  csv_path: data/interim/combined_DB22_measurements_sorted_clean.csv

model:
  name: et
  params:
    n_estimators: [400, 800]   # try two values
    max_depth:    [15, 25]     # and two depths
    random_state: 0
