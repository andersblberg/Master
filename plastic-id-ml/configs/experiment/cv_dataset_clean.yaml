# ---------------------------------------------------------------------
# Dataset
# ---------------------------------------------------------------------
data:
  csv_path: data/interim/combined_DB22_measurements_sorted_clean.csv

# ---------------------------------------------------------------------
# Model (these are changed from the CLI)
# ---------------------------------------------------------------------
model:
  name: et
  params: {}

eval_cv:
  n_splits: 10
  random_state: 42

# ---------------------------------------------------------------------
# Cross-validation control  <-  (5-fold CV)
# ---------------------------------------------------------------------
cross_val: 10          # integer = number of folds