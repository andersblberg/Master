data:
  csv_path: data/interim/combined_DB22_measurements_sorted_clean.csv

# ---------------------------------------------------------------------
# Model
# ---------------------------------------------------------------------
model:
  name: et          # <- "et" = ExtraTreesClassifier

  params:
    n_estimators: 800
    max_depth:    25
    random_state: 0
