# ---------------------------------------------------------------------
# Dataset
# ---------------------------------------------------------------------
data:
  csv_path: data/interim/combined_DB22_measurements_sorted_clean.csv

# ---------------------------------------------------------------------
# Model (you can change these as usual from the CLI)
# ---------------------------------------------------------------------
model:
  name: et
  params: {}          # or tuned hyper-params

eval_cv:
  n_splits: 10
  random_state: 42

# ---------------------------------------------------------------------
# Cross-validation control  ⟵  NEW (5-fold CV)
# ---------------------------------------------------------------------
cross_val: 10          # integer = number of folds